// Floating/sticky support widget is completely disabled per requirement.
// Support is exclusively accessible via the dedicated Support page (/support).
export function SupportWidget() {
  return null;
}

export default SupportWidget;
