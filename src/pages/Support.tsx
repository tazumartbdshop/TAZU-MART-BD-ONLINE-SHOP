import React from 'react';
import { SupportCenter } from '../components/ui/SupportCenter';

export default function Support() {
  return (
    <div className="w-full bg-white text-slate-900 animate-fade-in min-h-screen" id="support-standalone-page-wrapper">
      <SupportCenter />
    </div>
  );
}
